<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <form method="POST" action="<%= action %>">
        <div>
            <label for="categoryName">Category Name:</label>
            <input type="text" id="categoryName" name="categoryName" value="<%= category ? category.name : '' %>" required>
        </div>
        <div>
            <label for="categoryDescription">Description:</label>
            <input type="text" id="categoryDescription" name="categoryDescription" value="<%= category ? category.description : '' %>" required>
        </div>
        <button type="submit"><%= category ? 'Update' : 'Add' %></button>
    </form>
</body>
</html>
